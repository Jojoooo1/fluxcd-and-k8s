apiVersion: apps/v1
kind: Deployment
metadata:
  name: java-demo-database-deployment
  annotations:
    flux.weave.works/automated: "true"
    fluxcd.io/tag.java-demo-database: semver:~8

spec:
  template:
    spec:
      containers:
        - name: java-demo-database
          image: mysql:latest
