apiVersion: v1
kind: PersistentVolume
metadata:
  name: alertmanager-pv

spec:
  # storageClassName: standard
  # Two way binding
  claimRef:
    name: grafana-pvc

  capacity:
    storage: 10Mi
  accessModes:
    - ReadWriteOnce


  hostPath:
    path: "/mnt/data"